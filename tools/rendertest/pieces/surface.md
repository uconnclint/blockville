# surface — voxel faces & materials (src/render/voxel.js, src/render/materials.js)

## 2026-09-24 — round 1 (builder)
**Changed**
- materials.js FRAG_COLOR rewritten clean: removed per-voxel seam/panel grooves + slab lip, band-limited tonal mottling, sub-tile tooth + joint grid + Mikkelsen bump, per-floor banding, weathering run-off, contact grime, roughness noise. Param keys kept (grain/tooth/panel/grime/weather/floorVary... default 0) so setParams callers don't break. instVary 0.075 -> 0.03.
- Glass: day albedo pulled to a clear blue (`glassTint` #3d74d0, `glassTintAmt` 0.88; plain 'glass' palette 60%), glassMetal 0.80 -> 0.12, glassSky 2.7 -> 0.7, glassEnv 3.2 -> 1.1, glassTilt 0 (per-pane scatter read as a chequer), plus a soft two-band diagonal sheen (`glassSheen` 0.24, fwidth-faded). Before: panes washed to lavender/grey.
- Legacy res-1 windows only (new varying vVoxRes; wall faces only): clean white painted frame + sill (`winFrame`), no size/tone jitter, no mullion. res>1 models get NO procedural window dressing (their frames are geometry). Frame/sill never glow at night (emiAmt masked), panes still per-cell on/off + dusk ramp; CPU mirror unchanged.
- Conductors capped at metalness 0.35 (`metalMax`) so light-grey AC units/poles read light grey, not dark env mirrors.
- voxel.js: `greedy: true` default for all models (res-1 glow faces never merge -> window lattice/CPU mirror exact); `groundAO: true` — ground plane (y<0) is solid for AO samples, so every face meeting the ground gets the soft ref04 contact darkening per vertex (replaces shader grime); `seal: 0.0035` grows slice-mesher quads in-plane to close greedy T-junction pinholes. selfTests updated (pass, node + real GL materials selfTest).

**Measured**
- res-1 models (vehicles/props/trees, 112 variants): greedy cuts tris 20036 -> 13188 (-34%). All 162 catalog buildings are now res 4 (greedy was already auto there: 3.08M -> 1.45M, -53%).
- one-bakery / iso-close: 61 fps, 0 console errors. iso-mid fps 42-61 varies run to run with other pieces' concurrent model churn (tris 0.3M -> 1.6M over the session from new res-4 models), not from this shader (which got cheaper: no noise taps).

**Not mine, seen while verifying (for post / lighting owners)**
- Dark triangular dots along thin 1-voxel trim (bakery pilasters/cornice): confirmed SSAO (post.js contact term) — gone with ssao.enabled=false, persist with greedy off / AO changes. A faint shadow-map staircase remains on the same notch (lighting.js).
- Night: bluish-white diagonal smears on some res-4 blocks persist with all glass reflection terms zeroed -> not materials (glow billboards / bloom?).
- Yellow walls show faint soft blotches near trims = SSAO wide term.

**Next**
- Convex-edge highlight (ref04's crisp lit edges) via a per-vertex edge attribute from the mesher.
- Per-pane (not per-cell) night randomisation for res-4 panes spanning cells (needs a pane id from the mesher).
- Re-check glass against ref05 at iso zoom once lighting's final key/fill lands; maybe tint the sheen by sky colour.

## 2026-09-24 — round 2 (builder)
Critic r1 picked the reference: "dirty grey smudges/blotches" on the bakery parapet, roof well and cornice; black lumps; flat matte glass.
**Diagnosed** (A/B in one page, scratchpad vshoot.mjs on a snapshot server): the blotches were three things — (1) post.js SSAO on voxel faces (convex-edge halos + blotches on open faces: `debug:'ao'` showed it; gone with ssao off), (2) our own res-4 AO: the round-1 box term (one air layer, flat 2Rx2R box) gave every proud letter/trim a 1-unit grey halo, and triangle interpolation of non-planar corner AO creased along quad diagonals, (3) roof-well streaks = sun shadow penumbra (gone with lighting shadowStrength 0) — NOT surface, flagged for light.
**Changed**
- voxel.js: SOLID-ANGLE AO for reach > 1 (`aoMode:'solid'` default, `'box'` = r1): R layers deep, exp falloff laterally and in height (`aoFalloff` 0.5·R), separable (column sums -> A pass -> B pass), evaluated only over the layer's face bbox. Tall wall = classic crease level (selfTest 11 still 0.611), 1-voxel proud details only a thin tight band (selfTest 12). `aoLevels` 6.
- voxel.js: BILINEAR AO — slice mesher also emits `aoQuad` (4 corner factors, unorm8) + `aoUV` (corner 0/1); materials blends per fragment (no diagonal creases, triangulation-independent). Fallback to aoT when absent (props.js ignores them).
- voxel.js: face-bbox early-out per layer also speeds box mode (skyscraper 150 -> 86 ms).
- materials.js: voxel pixels write scene alpha = `ssaoKeep` (0) and post.js LIT pass does `ao = mix(1, ao, uAO * c.a)` (2-line integration edit in post.js — post owner please keep it). Terrain/roads/props keep full SSAO.
- materials.js: ao 0.95->1.0, aoDirect 0.38->0.8 (voxel AO is the only AO on buildings now), skyFill 0.16->0.28, new `skyFillDown` 0.5 (awning undersides stay coloured), `darkFloor` 0.03 (black trim keeps face tones), glass streak now mostly indirect SPECULAR (`glassSheen` 0.40) so it reads on shaded panes. `debugAO:true` renders the AO factor (harness).
**Measured**: iso-mid/iso-close/one-bakery 61 fps, 0 console errors. Catalog res-4 tris +3..+10% vs box (bakery 9164->8992, stadium 52.6k->60.8k), mesh time ~1.5-2x box (stadium 56->93 ms); city buildMs 3.3-3.6 s (unchanged within noise).
**Next**: roof-well streaks are the lighting piece's PCSS penumbra on the donut/sign shadow (visible at one-bakery) — biggest remaining smudge. Post: image still reads slightly soft at close zoom. Consider a per-pane (mesher-provided) UV for glass so the reflection streak is framed per window instead of world-diagonal.

### Coordinator note (2026-09-24 11:08) — AO ownership (4 critics now say "AO missing")
YOU own the baked per-vertex voxel AO (voxel.js) — the soft darkening in every
concave corner of a model: parapet inside corners, under cornices/sills/awnings,
wall-to-plinth. It is currently far too weak to see at iso-mid / one-bakery zoom.
Make it clearly visible like ref04 (a smooth gradient ~1 world unit wide, darkest
~0.55-0.65 at the crease, colour-preserving, never grey/dirty). Check the AO ramp
survives greedy merging and the material/lighting chain (materials.js may be
scaling it down — look). Shadow-acne striping on roof decks belongs to LIGHT
(told them). Post's SSAO only handles contact between separate meshes.

## 2026-09-24 — round 3 (builder)
Critic r2 picked the reference: "AO almost missing" (parapet inside corners, sign-band underside, awning/wall joints, wall/plinth line) + roof-deck diagonal striping.
**Diagnosed**: roof-deck striping disappears with lighting shadowStrength 0 -> cast-shadow acne, LIGHT's (coordinator already assigned it). Roof-deck/paving "blotches" that survive shadows+SSAO off are AUTHORED (deck uses 3 greys 81/83/84) — artists'. The weak AO was mine: both r1/r2 kernels are VOLUME counts, so a thin feature occluded almost nothing however close (1x2-voxel ledge: 4%; small parapet crease ~10%).
**Changed (voxel.js)**
- `aoMode:'ray'` default for reach > 1: per-lattice-vertex RAY-CAST AO. 32 cosine-weighted golden-angle rays (base set x 4 rotations about the normal -> exactly 50% into any axis-aligned half-space), aoDist 2.0 world units (8 voxels at res 4), hit weight (1-t/D)^0.3, ground plane solid. aoT = 1 - 0.78 * occ^0.85 (crease ~0.55, inside corner ~0.39), quantised LINEARLY in brightness (aoRayLevels 12) so ramps never end in a contour line. 'solid'/'box' still selectable.
- RAY TEMPLATES: every AO vertex is a lattice point, so each ray's cell sequence is identical per face direction — walked once (DDA) into (offset, weight) lists; per vertex a ray is a scan of occupancy loads. Plus SVT box early-out. Catalog mesh 1.09 s (solid) -> 1.28 s (16 rays) / 1.40 s (32 rays); per-vertex DDA was 3.5-4 s.
- TOLERANT MERGE (ray mode): same-colour rectangles merge when bilinear AO of the 4 corners reproduces every inside vertex level within 0.5 level (aoMergeTol). Catalog tris 1.04M (solid) -> 1.00M (ray) — AO wider AND fewer tris. In-game iso-mid 1.70M -> 1.64M tris, buildMs within noise of solid.
- selfTests 11/12 updated to the ray semantics (crease both 50% at the line; kerb band lighter one voxel out). All pass.
**Changed (materials.js)**: glassSheen 0.40 -> 0.55, glassSky 0.70 -> 0.90 (critic: glass flat blue).
**Measured**: 0 console errors in iso-mid/iso-close/one-bakery. FPS numbers this round are meaningless (load avg 7-8 from other builders' shoots; solid vs ray alternated runs: same fps band).
**Seen, not mine**: cast-shadow acne streaks on cream parapet tops and roof decks (light); lighting.js now has its own worldAO (aoRadius 3.0, aoDirect 0.6) multiplying on top of voxel AO — if buildings go muddy, the two must be balanced (light owns ground contact, surface owns corners).
**Next**: per-pane glass gradient needs a mesher pane id/UV for res-4 panes; face-tone separation (top/left/right) is light's ratio but skyFill 0.28 in materials flattens it — co-tune with light.

## 2026-09-24 — round 4 (builder)
Critic r3 picked the reference: "light faces look dirty" — streaky grey-green smears on white cornice tops / parapet caps / ledges and pale lot paving, faint checker on the roof deck.
**Diagnosed** (A/B variants on one-bakery, r4-diag): the streaks survive with SSAO off and with shadows off, and vanish only with lighting.js's height-volume **world AO** off as well. That term (8 directions x 2 taps against a 2048² top-down height map) is not smooth on a flat face. It was multiplying on top of our baked AO on every voxel pixel. Cast-shadow edges added the rest (light's). The remaining deck "checker" is AUTHORED: commercial.js:322 alternates C.concrete / C.offwhite in 4x4 blocks on shop roof decks. That's the commercial artist's call, so I left it.
**Changed**
- materials.js: new `worldAOKeep` (default 0). On voxel faces the shader pre-seeds lighting.js's per-fragment cache `csmAoVis` before the lighting loop (only when patched, `#define VOX_CSM`), so voxel pixels skip the world AO and its 16 taps. Terrain, roads and water still get it. Fill saturation (`sat`) is unaffected.
- voxel.js: `aoRayToe` 0.16, a soft-knee toe on the ray-AO occlusion (quadratic below 2·toe, so no onset line), with `aoRayCurve` 0.85 -> 0.66 so the crease stays 0.57 and the inside corner 0.38. Faintly occluded open faces (a roof deck inside a parapet, paving by a planter) now stay pure albedo. Before, they went a uniform light grey. selfTest passes (corner 0.380, res-2 crease 0.545).
**Measured**: one-bakery 61 fps, iso-close 56, iso-mid 36 (13 at baseline this session: load from other builders' shoots). 0 console errors. Night emissive windows fine (setNight pinned to 1).
**Seen, not mine**: (1) the roof-deck 4x4 checker (commercial.js:322, artist). (2) Sidewalk/plaza under the market stalls still has low-frequency mottling, and a dark dotted line runs along the lot kerb on the sidewalk. These are non-voxel surfaces (world AO + SSAO contact on terrain/roads): light/post/roads. (3) Frame softness is post's AA.
**Next**: if light wants building-to-building contact back on voxel faces, give it a smooth (blurred / more-direction) world AO and raise worldAOKeep to about 0.3. Otherwise add neighbour-model occupancy to the ray AO at placement time.

## 2026-09-24 — round 5 (builder)
Critic r4 picked the reference: "blocky square-texel grey smudges" on the lot/plinth tops, paving and kerbs (bakery patio edge, tree-planter base, kerb strip, market paving).
**Diagnosed** (A/B on one-bakery and iso-close; the pick raycast is in scratchpad rounds/surface/pick_eval.js): almost none of the smudged surfaces are voxel. The market paving, planters and their trees are `terrain-lots` (terrain material), and the walk and kerb are `bv-roads`. The bakery patio floor is voxel, and it is clean in every variant. There were three sources:
 (1) lighting.js world AO (square height-map texels). The coordinator already defaulted it OFF (`worldAO:false`) mid-round.
 (2) post.js SSAO contact term: a ragged, dotted 1-2 px dark seam on the walk along the foot of every voxel lot plinth (gone with SSAO off).
 (3) CSM cast shadows of the terrain tree canopies: a stepped, streaky dark blob with a bright gap line on the paving. It is gone with shadowStrength 0, and the steps follow the canopy's serrated bottom band. Softer PCSS, lower bias and a higher penumbra floor do not fix it (r5-sh/).
**Changed** (integration edit in post.js, ~10 lines; it extends the r2 ssaoKeep contract): SSAO now also gates the OCCLUDER by scene alpha. `ssao.castGate` (default 1) sets how much of `texture2D(tScene, suv).a` weights each occluding sample, so voxel pixels (ssaoKeep 0) neither receive nor cast screen-space AO. Their AO is baked by voxel.js, with the ground plane included. The alpha fetch sits after the depth/plane rejections, so only real occluders pay for it. Terrain, roads and props still cast and receive as before. voxel.js and materials.js are unchanged this round.
**Measured**: the walk beside each plinth is clean now (r5-sb gate0 vs gate1, r5_sb1.png). post and materials selfTests pass. iso-mid, iso-close and one-bakery run at 61/61/60 fps with 0 console errors. An alternating castGate 0/1 fps A/B shows only load noise.
**Seen, not mine (please route)**:
 - LIGHT: the stepped, streaky tree-canopy shadow on light paving (r5_tree.png, r5_sh.png) is now the main remaining smudge. At night, large dark triangular wedges cross building facades. They persist with SSAO off and greedy off, so they look like lamp point-light shadows (r5_night2.png).
 - GROUND: the football pitch in iso-close shows grey diamond z-fighting. The raycast finds TWO coplanar lot surfaces at y 0.932 plus paving at 0.920 in the same spot (r5_pitch.png). Terrain lot trees and planters could also write scene alpha 0 (like voxel) to drop their SSAO skirts on paving.
 - POST: the letter and frame edge fringe on the BAKERY sign (1 px dotted mid-tone) is AA/CAS.
**Next**: if contact on the ground beside voxel models is wanted back, bake it in voxel.js (a ground skirt with the same ray AO, multiply-blended) instead of using screen-space AO. Per-pane glass gradient is still open.

## 2026-09-24 — round 6 (builder)
Critic r5 picked the reference: "blotchy grey smears/streaks on the white cornice/parapet; white right-hand faces go muddy grey instead of a clean tinted darker tone; glass = flat blue + white diagonal stripes (cartoon hack)".
**Diagnosed** (r6-diag/, same run A/B on one-bakery): the smears vanish with `shadowStrength 0` and survive SSAO off, and debugAO shows the baked voxel AO is smooth. They are the CSM term: the low-contrast tails of the PCSS filter (s ~0.6-0.95). The engine.js call site sets `minPenumbra 0.30` (0.3 world units is about 6.5 texels at cascade 0), which blurs the shadow-map texel staircase and PCF leak around small rooftop props into grey clouds. With minPenumbra 0.06-0.1 (r6-pen/), the true shapes show up as serrated sawtooth fragments. The muddy side: our unlit (N.L<0) white measured 148 against 251 on top (0.59). ref05's white dark side is ~0.82 and ref04's cream is 0.76 (and MORE saturated, warm).
**Changed (materials.js only)**
- `shadowCrisp` [1, 0.22, 0.80]: on voxel faces the shader re-shapes csmLastShadow with smoothstep(lo, hi). It scales direct diffuse+spec by g(s)/s (bounded ≤1.25, g(0)=0) and writes the new value back so the glint agrees. Umbrae and shadow shapes stay the same. Faint smudges/leaks (s>0.8) go fully lit, and the blurred staircase becomes a soft, clean contour. It is off at night (uNight). Terrain/roads are untouched.
- `bounce` [0.30, 0.55] + `bounceTint` #fff1e0: sun bounce into faces that turn away from the key (clamp(0.30 - N.L)), scaled by directionalLights[0].color, so it fades at dusk and is ~0 at night. It carries albedo hue (hue = albedo/luma, 55%), so a coloured dark side gets richer rather than grey. Glass takes half. Measured on an iso-mid tower: shaded wall (77,112,123) -> (105,151,156), shaded/lit 0.54 -> 0.72 (ref04 0.76). The white shaded face is lifted and warm instead of blue-grey.
- `glassSheen` 0.55 -> 0.40 (stripe subtler, a midpoint with the r2 "little reflection" critic).
**Measured**: materials selfTest pass (in-page). 0 console errors in iso-mid, iso-close, one-bakery, iso-night and golden. Night windows are unchanged. dpr-1 fps new/old alternating: 61/61 and 32/30 vs 56/61 and 30/56 (load 4-9, so noise). The new terms are a few ALU ops.
**Not mine, please route to LIGHT**: the remaining roof-deck shadow blobs are the sign/AC shadow drawn with a 0.3-unit PCSS floor. Their ragged, streaky left edges come from a per-pixel blocker-search variance (blockerSearchWorld 2.2). A proper fix is a higher-res cascade 0 (tile 2048, or a tighter fit at iso zooms) plus minPenumbra ~0.12-0.15. `minPenumbra 0.15, maxPenumbra 1.2, blockerSearchWorld 1.2` looked slightly cleaner in r6-v2 but I did not change engine.js. The "stray black line near the donut sign" was a lamp-arm cast shadow on the road (it went away with shadows off). Models keep changing mid-round (the bakery was re-authored twice during this session), so compare within one run.
**Next**: per-pane glass UV from the mesher (aoUV already carries quad-corner coordinates; a merged glass quad = one pane) for a subtle top-dark/bottom-light pane gradient like ref04's inset windows. Re-check the crisp curve once light lands a sharper penumbra (lo/hi can then relax toward [0.1, 0.9]).

## 2026-09-24 — round 7 (builder)
Critic r6 picked the reference: "smudgy dark halos along every silhouette and under ledges (cornice, sign boxes, bush, planters) + crunchy sharpening; glass = flat blue with painted diagonal streaks".
**Diagnosed** (r7-diag/, one-bakery, same run): the halos are post.js SSAO on voxel faces. Post r5 set `ssao.voxelKeep 1`, which overrides our ssaoKeep 0. With `ssao.enabled false` they are gone (g1.png). With edge/sharpen/usm off the image barely changes. debugAO shows the baked ray AO is smooth, but aoDist 2.0 with fall 0.3 is near-binary out to 8 voxels, so every cornice still laid a broad grey skirt on the wall below.
**Changed**
- post.js (integration, 1 value + comment): `ssao.voxelKeep 1.0 -> 0.0`. Voxel faces take no screen-space AO again (they still CAST it onto terrain and roads, castGate 0). The baked AO already covers lot contact, since every building brings its own plinth. POST: please keep this. If you need SSAO back on voxels, do it only with a strict crease-only range check.
- voxel.js AO: `aoDist 2.0 -> 1.5`, `aoRayFall 0.3 -> 1.0` (linear), `aoRayStrength 0.78 -> 0.80`. The crease stays about 0.53-0.56 at the line (selfTest: res-2 crease 0.533, corner 0.380) and the gradient now sits tight in the corner, like ref04. The variants A-D are in r7-v/.
- voxel.js: new `paneUV` attribute (slice mesher, glass class on walls only). It holds the position inside the whole connected pane, found by walking the occupancy grid, so the value is continuous across the glow-lattice and AO splits. It is stored as unorm8 1+254t, where 0 means none. Node check: a 5x6 window split into 4 quads gives continuous 1..255.
- materials.js: removed the two hard diagonal glass streaks. The replacement is one broad, soft band per ~3 tiles (glassSheen is unchanged). Added `paneGrade [0.74, 1.12, 0.14]` (head, sill, jamb): each res>1 pane deepens toward the head and clears toward the sill, with a faint jamb darkening. The mirror fresnel also clears toward the sill. `vVoxAOUV` is now a vec4 (aoUV and paneUV share one varying row).
**Measured**: selfTests pass (voxel in node and page, materials in page). iso-mid/iso-close/one-bakery/iso-night have 0 console errors. The night windows are fine. one-bakery runs at 55-61 fps. iso-mid ran 2-21 fps under load 6-7.5 (other builders' Chromes); the diag base under the same load was 21, so this is noise.
**Next**: (1) the sign box's dark brown right face still reads muddy, because the bounce only lifts by albedo. A dark-albedo floor on the shaded side is one option, or the shops artist could lighten the brown. (2) The roof-deck cast-shadow shapes are light's. (3) Check that paneUV panes read at iso-mid on glass towers. Curtain walls merge into huge panes, so the grade spans the whole wall there. It may need a cap (a pane taller than N cells gets the grade per cell row).

## 2026-09-24 — round 8 (builder)
Critic r7 picked the reference. The bakery looked "smeared, like an oil-paint/denoise pass": ragged, blotchy shadow edges on the roof, red wall and walk; no soft AO in inside corners (cornice, reveals, awnings, plinth); flat saturated glass with no highlight; left and right faces too close in value.
**Diagnosed** (r8-diag/, one-bakery, same run): it is NOT post. `debug:'raw'`, detail/usm/sharpen/edge off and ssao off all keep the smear, and shadowStrength 0 removes it completely (noshadow_nossao looks razor-clean). The ragged edges are the CSM texel staircase plus tap noise at the default 4-device-px penumbra floor. My shadowCrisp is not the cause (nocrisp looks the same). Baked AO worked, but at crease ~0.56 with a linear ramp a small overhang (occ 0.15-0.25) only darkened a lit wall ~3-5%, and the lit faces sit on the tonemap shoulder (pink R pinned at 247).
**Changed**
- engine.js call site (integration, 1 value + comment): `penumbraFloorPixels: 10` (lighting default 4). The staircase becomes a clean soft edge (g4/g5.png), and taps stay capped at CSM_MAX_TAPS. LIGHT: feel free to own and tune this, but keep voxel-wall shadow edges free of the sawtooth.
- voxel.js AO: new `aoRayKnee` 4, a saturating shape (1-e^-kf)/(1-e^-k). Also aoDist 1.5 -> 2.0, aoRayFall 1.0 -> 1.5, aoRayStrength 0.80 -> 0.66 (floor 0.34, so deep reveals no longer crush to near-black), aoRayCurve 0.66 -> 1.0, aoRayToe 0.16 -> 0.05. Small overhangs now lay a visible soft band (cornice→wall G 135→109 vs →123 before), and reveals stay ~80 (were 42 in a no-knee strong variant). selfTest 11 tolerance relaxed (res-2 crease now 0.45 vs res-1 0.61, floor 0.34). Same tris (+0.25%), city buildMs ~+13%.
- voxel.js paneUV: the pane is now the flood-filled CONNECTED glass-CLASS component in the face plane (bbox, cached per face+cell). The r7 cross walk used the same colour only, so authored two-tone / L-shaped windows (supermarket) broke into zigzag glints. Node check: a 6x8 two-tone window gives continuous 1..255 with 0 mismatches.
- materials.js glass: the world-space band is replaced by a PANE-LOCAL soft "/" glint (one broad band plus a faint thin one), in the pane's metric frame (size from derivative ratios), oriented to screen-right on both wall directions, repeating every ~1.6 units on big panes, and faded below ~8 px panes. glassSheen 0.40 -> 0.60, glassTint 0x3d74d0 -> 0x4f8fe0 (panes measured navy 18,60,120), paneGrade [0.74,1.12,0.14] -> [1.06,0.92,0.10] (baked AO now shades the head).
- materials.js: aoDirect 0.8 -> 1.0, bounce 0.30 -> 0.22 (right/left face luma 0.79 -> ~0.77, ref04 0.76).
**Measured**: materials + voxel selfTests pass (page + node). 0 console errors on iso-mid/iso-close/one-bakery/iso-night, and night windows are fine. At dpr 1 (load 6-11): iso-mid 41-52, iso-close 57, one-bakery 61. The dpr-2 numbers under load are noise (the same config gave 51 and 16 back to back).
**Next**: (1) the right-face value separation is mostly the key direction (light), and both bakery walls are sunlit at the current key azimuth. (2) The supermarket's authored light-blue checker in its glass reads as busy with the glint on top. That is the artists' call, but they could drop it now that glass has its own reflection. (3) If critics want even more corner AO: raise aoRayStrength to about 0.72 (floor 0.28) before touching the knee.

## 2026-09-24 — round 9 (builder)
Critic r8 picked the reference. On the bakery's pink storey the left and right faces were almost the same pink, and the AO was only a thin line under the cornice and sills and at the sign-band ledge. Secondary: rooftop whites and pale lot looked overexposed and flat.
**Diagnosed** (r9-v2, same run): the key already leaves the right wall (fill only), but right/left luma was still 0.85 (ref04 0.73; ref04's shaded side sits at ~105 luma against ~146 lit). The horizon-uniform hemi and sky IBL gave both walls the same indirect light. envIntensity 0 alone moved it to 0.74. For AO: the ray AO is physically right for a 1-voxel res-4 ledge, and that only occludes a sliver of sky a voxel below it. On lit faces the lit pink sits on the tonemap shoulder (R 251), which hides a small AO dip.
**Changed**
- materials.js: new `shadeSide [0.40, 0.30, 0.45]` (uShadeSide). On faces turned away from the key (smoothstep of -N.L up to 0.45) it scales indirect diffuse by (1 - 0.40) and carries the albedo hue by 0.30, so the dark side is a deeper, MORE saturated version of the lit colour, not grey. Tops, lit walls and glass are untouched. It is off at night. Pink right/left luma 0.85 -> ~0.70-0.73 (ss25/35/45 sweep measured 0.70/0.67/0.63 at an AO-banded point; 0.80/0.76/0.72 on open wall).
- voxel.js: new `aoSpread 1.0` (world units) + `aoSpreadGain 0.75`. This is a soft separable dilation of each face layer's ray-AO darkness across the face plane: max(own, gain * neighbour * (1 - smoothstep(d / (r+1)))). It is a pre-pass over the layer's face bbox that stamps vtxV, so the mask/merge code is unchanged. `aoRayStrength 0.66 -> 0.76` (floor 0.24). Every contact now lays a soft band ~3-4 voxels wide: the lit pink under the cornice goes 155 -> 185 over ~20 px at dpr 1, where r8 was flat 187. The right wall has a volumetric ramp. The storefront under the awnings and the sign band under the storey overhang also darken softly. I saw no halos on letters or frames at iso-close.
- The smoother AO field merges BETTER: catalog (261 models) tris 3.09M -> 2.82M (-9%), with mesh time the same within noise (18.5 s vs 18.9 s under load 9).
- selfTest 11: the floor is now 1 - aoRayStrength and the tolerance 0.3 (res-2 crease 0.367). selfTest 12 is built with greedy:false and probed 4 voxels out, because spread makes every contact identical out to ~2 voxels by design (kerb 0.937 > wall 0.873). All pass (node). The materials selfTest passes in-page.
**Measured**: 0 console errors on iso-mid, iso-close, one-bakery and iso-night. Night windows are fine. fps is meaningless this round (load avg 8-9.7: dpr-1 iso-mid 19, one-bakery 35). The new shader term is ~10 ALU ops, and tris went down.
**Tooling**: scratchpad r9/ab.mjs does in-page A/B with a remesh. --pre swaps engine._geoCache for a Map, and each variant rebuilds every cached model with new voxel defaults. The camera and lighting stay identical across variants (separate boots drifted in framing and sun).
**Not mine**: the lit pink/white faces sit on the tonemap shoulder (R 251, white tops 241), so AO and top/left separation get compressed on bright albedos. That is exposure (light/post). ref05's highlight distribution is actually brighter than our iso-mid (p99 253 vs 243), so I left it. The bakery was re-authored twice more mid-round (sign side flipped, then the storey shortened).
**Next**: if a critic calls the bands dirty, drop aoSpreadGain to ~0.6 before touching the radius. If they want more right-face depth, raise shadeSide to 0.45 (ratio ~0.70). A tonemap-aware AO (apply part of the AO after the tonemap) would make bands read on bright lit faces. That needs a post hook.

### Coordinator note (2026-09-24 19:15) — the dirt was NOT your AO; make voxel AO visible
Earlier "dirty blotches" were traced to OTHER modules and are now fixed (lighting
worldAO off, shadow bias, over-wide penumbra; post sharpen/usm/detail off). So stop
holding your voxel AO back. r5/r8/r9 all say inside-corner AO is "too faint and
tight". Target on a flat wall meeting a ledge/cornice/sill/ground: a smooth ramp
~0.8-1.2 world units wide (res 4: 3-5 voxels), darkest multiplier ~0.60-0.65 right
in the crease, colour-preserving (darker saturated wall colour, not grey). Verify by
sampling pixel values across a bakery cornice in one-bakery at 2x (PIL line profile)
and compare to ref04's under-cornice gradient.

## 2026-09-24 — round 10 (builder)
Critic r9 picked the reference. Biggest gap: inside-corner AO on the bakery facades was "too faint and too tight" (sills, flower boxes, cornice, beside the pilasters), and the lit left pink face was "nearly as bright as the roof". Secondary: glass was flat mid-blue, and ground contact was weak.
**Diagnosed** (r10-a..h, in-page A/B; r10/probe.mjs gives synthetic res-4 facade AO profiles in node): the fine AO field is not weak per contact (crease 0.37, ~1-unit band), but on a dense res-4/8 facade every contact gets the same band. The bands overlap, so the wall reads as one evenly lit, slightly darker tone with no hierarchy. The lit wall also took the key at ~ the same N.L as roofs (engine -75 az / 40 el: wall 0.66, roof 0.64), and it sits on the tonemap shoulder. Measured cream top/left/right 227/216/149 (1 : 0.95 : 0.66). ref04 same-albedo is 1 : 0.89 : 0.63.
**Changed**
- voxel.js: new BROAD AO (`aoBroad 0.5, aoBroadDist 3.5, aoBroadFall 1, aoBroadKnee 2, aoBroadTop 0.5`). It is a second, massing-scale term: cosine-weighted rays with transmittance through a coarse density grid (1 cell per world unit, ground solid), cached lazily per face direction at the cell centres, and read trilinearly 0.5 units off each face. It is multiplied onto the fine AO. A 1-voxel frame barely registers, while the parapet-roof, cornice-wall, wall-plinth and storey-overhang corners pool widely. Up-facing faces take half (at full strength the patio went dusky grey in r10-g). It is gated to building-scale callers (aoDist >= 1), so props.js veg/rocks (aoDist 0.3) are unchanged. Cost is ~3% of mesh time (profiled).
- voxel.js: `aoSpread 1.0 -> 1.5`, `aoSpreadGain 0.75 -> 0.9`. selfTest 11 builds its res-2 twin with aoBroad 0 (broad is not part of the res-family match). All selfTests pass (node).
- materials.js: new `wallKey 0.70` (uWallKey). By day it scales the key's DIRECT diffuse on vertical faces, so a lit wall sits a clear step under the tops. The shadow direction, tops, fill hue and specular are untouched, and it is off at night. 0.80 moved the lit pink only 3% (shoulder + fill).
- materials.js: `paneGrade [1.06,0.92] -> [1.16,0.86]` (a stronger sky-to-sill grade per pane).
**Measured**: lit-pink luma on the bakery storey (r10-f pinkstat) p50 154 -> ~127 with pools, and the right face is still darker. Patio region mean 142 (r9 settings) -> 131. iso-mid tris 2.76M (critic r9) -> 2.30M: the smoother field merges better. 0 console errors on iso-mid, iso-close, one-bakery and iso-night, and night windows are fine. fps is meaningless this round (load avg 20-190 from other builders' Chromes; one-bakery 16-25, iso-mid 6).
**Tooling**: scratchpad r10/ab.mjs is r9/ab.mjs that remeshes only the geometries in the camera frustum. Full-city remeshes of the new res-8 models blew the 600 s timeout.
**Next**: if a critic calls the facade too dark or dirty, back off in this order: wallKey 0.75, then aoSpreadGain 0.8, then aoBroad 0.4. If they want more ground contact on the lot, raise aoBroadTop toward 0.7. The terrain grass rim around each plinth is post/ground (SSAO castGate), not voxel.

### Coordinator note (2026-09-24 20:22)
Your wallKey and the light builder's key-angle work target the same ratio. Light has
been told to use your 1 : 0.89 : 0.63 and to measure after wallKey. Re-measure with
tools/rendertest/faceprobe.js (white cube; recipe in light.md 18:15) before tuning
wallKey further.

## 2026-09-24 — round 11 (builder)
Critic r10 picked the reference. Biggest gap: AO "too weak and too EVEN" on the bakery (pink storey flat under sills, cornice and trim; no contact gradient at the facade base or the lot plinth). Secondary: a stray vertical light/dark band on the pink facade, a slightly soft image, and glass that is streaky but not glossy.
**Diagnosed** (r11/probe8.mjs is a synthetic res-8 facade AO profile; the r11-a ratio map is base / no-AO luma in-page): (1) The r10 BROAD term was biased. It sampled a 1-unit density grid trilinearly half a unit off the face, so the cell BEHIND the sample held part of the wall itself. Every open wall sat at ~0.87 whatever was in front of it, and the value stepped with the facade depth inside a cell. That flattened the wall, and a recess crossing a cell boundary gave the vertical band. (2) The 1.5-unit smoothstep spread merged every band on a dense res-8 facade (features every ~2.5 units), so there was no open wall left for contrast. The rendered AO ratio on the pink wall ran 0.64-0.83 everywhere. (3) Measured: the speckle on every shaded wall foot and plinth side is three's material `dithering`. The scene RT is half-float, so the ±0.5/255 linear dither only gets amplified by sRGB and the grade.
**Changed**
- voxel.js: BROAD AO is now a half-space-clipped CONE TRACE on the fine summed-volume table. It uses 12 cos-weighted rays from a point lifted `aoBroadLift` 0.125 off the face. The boxes grow with distance (`aoBroadCone` 0.3) and are clipped to the region in front of the face plane (so no self-occlusion), and the ground at y<0 is solid analytically. It is evaluated on a res/2 sub-lattice with a per-layer typed cache and bilinear blending, with an early out when nothing is in reach. aoBroad 0.5 -> 0.6. Open walls are exactly 1.0 again.
- voxel.js: aoSpread 1.5 -> 1.0 (still smoothstep). The new `aoSpreadPow` option (0 = smoothstep) is for a convex falloff. Probe (res 8): under a 1-voxel sill 0.37/0.49/0.68/0.87/1.0 at 0/.25/.5/.75/1 u. Wall above plinth 0.24/0.30/0.49/0.62/0.81/0.87/0.94 at 0...1.5 u, so the ground contact is now wider than trim (hierarchy).
- materials.js: `dithering: false` on the voxel material (speckle gone; see r11-d/noise.png). Glass keeps only half of the baked AO (`mix(voxAOv, 1, 0.5*voxGlass)`), so the frame head no longer greys the top of every pane to matte.
**Measured**: selfTests pass (voxel node, materials in-page). 0 console errors on iso-mid, iso-close and one-bakery. Night windows glow. Catalog mesh time is +~25-30% (node, load ~9: 47 s -> 60 s) and tris +5%. City buildMs in shoot is unchanged within noise (87.5 s -> 89.9 s). fps is noise under load (one-bakery 22-28).
**Not mine / seen**: terrain.js, roads.js and props.js also set `dithering: true`, so the grass beside plinths shows the same speckle (a one-line fix for those owners). At night (BVDEMO.night(1), ab framing) large pale wedge-shaped lit regions cross the bakery's left facade and roof. They look like lamp/moon light volumes (light piece). The bakery was re-authored twice mid-round.
**Next**: if a critic still calls the facade flat, raise aoSpreadGain 0.9 -> 1.0 before widening. If "dirty", aoBroad 0.6 -> 0.45. The cone trace is ~30% of mesh time. Next saving: 8 rays, or skip it where the fine AO is already at the floor.

## 2026-09-24 — round 12 (builder)
Critic r11 picked the reference. Biggest gap: bakery walls "flat and a little washed out". There was almost no soft shading under the sills, cornice and pilasters, or where walls meet the roof and plinth. The mouldings looked like painted lines. The dark side was greyish, where ref04's is a rich warm orange.
**Diagnosed** with a new tool, scratchpad r12/aomap.mjs. It rasterises the baked aoT of one face direction of a catalog model (front-most wins) and prints column and row profiles.
- (1) The symmetric max-dilation spread (aoSpread 1.0 / gain 0.9) was the flattener. On a res-8 facade the window reveals floor at 0.24. Dilating that over 1 unit stamped ~0.45 over the WHOLE panel between the windows. Raw ray AO alone leaves the panel at 1.0, with gradients only at the edges. Capping or blurring the spread just moved the plateau (tried both; plateau at the cap).
- (2) The lit pink sat at R 249, on the display clip, so AO dips there barely showed.
- (3) ref04 measured (PIL): lit wall (200,125,69), luma 137. Shade (165,90,46), luma 103. Its walls are FLAT colour away from features; the soft shading is local to geometry.
**Changed**
- voxel.js: the new SKY SHADOW term (`aoSkyShadow 0.85, aoSkyReach 1.5, aoSkyPow 1.3`). rayOcc also records the occlusion of the UPWARD rays of wall vertices (occUpLast). That darkness is dilated DOWN the wall only, over 1.5 units with a (1-x)^1.3 falloff, and gets a short lateral soften. So every ledge (cornice, belt, sill, lintel, awning) lays a one-sided gradient on the wall below it, while the wall above a sill and between windows stays open. `aoSpread 1.0 -> 0.4` (the short symmetric contact band only). Gated to aoDist >= 1, like broad, so the veg/rock AO in props.js is unchanged. Also `aoRayKnee 4 -> 5`, `aoRayToe 0.05 -> 0.03`, `aoBroad 0.6 -> 0.8`, `aoBroadDist 3.5 -> 4.5`. selfTest 11 tolerance 0.3 -> 0.35 (res-2 crease 0.30). All selfTests pass (node).
- materials.js: new `aoHue [1.0, 0.0]` (uAOHue), a colour bleed into the AO. Occluded pixels are multiplied toward albedo/peak-channel, so a crease on pink goes deeper and redder, never grey, and whites and greys are untouched. A luma-normalised hue was tried first (r12-c). It pushed a lit pink's R past the clip, so the AO turned into a hue shift with no darkening. Do not use luma normalisation on lit faces.
- materials.js: `aoDirect 1.0 -> 1.5`. Values > 1 are now an exponent, pow(ao, aoDirect), on the key's direct diffuse, so AO survives the display shoulder on sunlit walls. Specular and glint clamp it to 1. `wallKey 0.70 -> 0.60` (lit pink off the clip). `shadeSide` hue carry 0.30 -> 0.60 and `bounce` hue carry 0.55 -> 0.85 (the warmer, more saturated dark side the critic asked for).
**Measured**: r12-e pink lit-face luma p10/50/90 went 120/151/159 -> 87/139/150 (real shading range; ref04 lit wall 137). Lit/shade 139/102 = 0.73 (ref04 0.75). r12-f/cmpz.png shows old vs new side by side: soft gradients under the cornice, belts and sills, and beside the pilasters. Catalog (261 models): tris 2.974M -> 2.993M (+0.6%), mesh time 75 s -> 59 s (the smaller spread radius; load ~13). Shoot: 0 console errors on iso-mid, iso-close, one-bakery and iso-night, and night windows glow. fps is load noise (other builders' Chromes, dpr 2).
**Not mine / seen**: iso-close has a strong crunchy outline/sharpen look (dark halos on every silhouette) that is not in one-bakery. That is post's edge/sharpen, so please check it. The "floating flowers" off the r11 bakery roof are ground veg behind the lot, seen past the roof edge (props/veg). The bakery was re-authored ~3 times during this round, so compare within one run (r11/ab.mjs).
**Next**: if a critic calls the facades dirty or dark, lower aoSkyShadow 0.85 -> 0.7 first, then aoSkyReach 1.5 -> 1.25 (the r12-f "newS" variant). If they want more, raise aoSkyPow toward 1.0 (a longer tail). Keep the spread symmetric radius small, because the max-dilation plateau is what flattened r9-r11.

### Coordinator note (2026-09-24 22:10) — broad AO overshot; aim for the midpoint
r9-r11: "AO too faint". r12 (with aoBroad 0.5): "ground-floor faces fall off into a
heavy muddy dark-red/near-black AO gradient toward the plinth". Halve the broad term
near the ground (it's stacking with ground contact + lighting's contact shadow) and
cap the combined AO floor at ~0.65 of lit value on vertical faces; keep the wide
soft pooling under cornices/sills that r11 asked for. The edge "oversharpened halo /
ringing" is post's sharpening (they've been told to gate it off at close zoom) — not
yours.

## 2026-09-24 — round 13 (builder)
Critic r12 picked the reference. Biggest gap: the ground-floor storefront fell into a "muddy dark-red, almost black" AO gradient toward the plinth, and the recessed cafe was crushed. Secondary: edge halos (post), glass "diagonal white slashes" and magenta/orange specks, and jaggy BAKERY letters and stair-stepped awnings (model geometry, shops piece).
**Diagnosed** with r13/st.sh, which is aomap.mjs plus per-face stats on the bakery. Baked AO: 25% of the +z facade pixels were <= 0.28 and the storefront p25 was 0.24, the ray floor. By itself, the fine term had a plain crease at 0.30, because strength 0.76 with knee 5 saturates. Sky and broad (ground at full weight, 4.5-unit reach) then stacked on it, and aoDirect 1.5 took the direct light to ~0.12.
**Changed**
- voxel.js: aoRayStrength 0.76 -> 0.55 and aoRayKnee 5 -> 2.5 (crease ~0.57, small ledge ~0.76). aoSkyShadow 0.85 -> 0.8, aoSkyReach 1.5 -> 1.25. aoBroad 0.8 -> 0.45, aoBroadDist 4.5 -> 3.5.
- voxel.js: new `aoBroadGround 0.35` scales the analytic ground in the broad cone trace.
- voxel.js: new COMBINED FLOOR `aoWallFloor 0.55 / aoTopFloor 0.5 / aoFloorKnee 0.1`, an exponential soft roll-off applied after every term is combined. It is gated to building scale (aoDist >= 1), so props veg is unchanged.
- materials.js: aoDirect 1.5 -> 1.25, aoHue [1,0] -> [0.6,0], shadeSide depth 0.40 -> 0.34, glassSheen 0.60 -> 0.45 (the midpoint of the r3-r8 swings).
**Measured**: baked bakery +z storefront p5 0.24 -> 0.54, min 0.24 -> 0.54. Open walls are still 1.0 and the soft bands under sills, cornice and pilasters remain (r13/v6.png). In-page ratio vs no-AO (r13/ab2): p5 0.20 -> 0.81, and the display compresses it. voxel selfTest passes. Shots iso-mid, iso-close and one-bakery: 0 console errors. iso-mid tris 3.06M. fps is load noise.
**Not mine / seen**: (1) The near-black band with green speckle along the bakery lot's right edge is NOT voxel material (it stays black in the uDebugAO view). It is terrain/ground (a skirt or trench beside the plinth, plus dithering: true). (2) Between my base and a shots (~17:50) the global black level lifted (road p10 27 -> 41) and the image went pastel. That is post/grade, and it makes AO read fainter in-page than the baked numbers. (3) The magenta/orange "specks" in the shop glass are authored model voxels behind the panes (shops). Letters and awnings are also geometry.
**Next**: if a critic calls it too faint again, raise aoRayStrength 0.55 -> 0.62 BEFORE touching the floor. Keep aoWallFloor >= 0.5, which is what prevents the muddy regression. If the recessed cafe still reads dark, lower aoSkyShadow before aoBroad.

### Coordinator note (2026-09-24 22:40) — GLASS reads pale across all building pieces
civic r10: "windows are low-contrast blue-grey slits with no dark glass"; downtown and
shops critics have said similar. That's the shared glass material (materials.js
sky-mirror / paneGrade), which affects every artist's buildings. ref05/ref04 windows:
a clearly DARKER saturated blue pane (~#2a5aa8 to #3a78c8 in rendered pixels) with a
lighter diagonal/top highlight band (~#8fd0f0), framed by a light frame. Sample our
rendered glass pixels in one-bakery / gal-downtown-1 with PIL and bring them into that
range — darker body, crisp highlight, higher contrast against the wall.

## 2026-09-26 — wave 4 round 1 (builder)
Picked up an interrupted earlier attempt at this round. It had already left `aoWallCap [0.34, 0.12]` (a soft cap on wall AO darkness), per-row pane grading on tall curtain-wall panes, and a `glassHi/glassHiP` head-highlight hook (amount 0 = off) in materials.js. I kept all three.
**Changed (materials.js)**
- GLASS (coordinator 22:40 "pale everywhere"): `glassTint 0x2272c8 -> 0x1872b0`, `glassDiffuse [0.7,0.7] -> [0.45,0.5]`, `glassReflTint 0.85 -> 1.0` (a grey sky mirror lifted R). A/B in-page (scratchpad tools/ab.mjs, v_g3/v_g4). One-bakery pane median: lit (82,126,220) -> (73,116,177), shaded (51,83,148) -> (45,76,114). ref05's shaded glass is (37,84,114) and ref04's is (47,77,122). iso-mid blue p50 (45,116,220) -> (62,116,166), inside the coordinator's #2a5aa8-#3a78c8 range. I tested the head-highlight band (glassHiP amount 0.3-0.45): it paled the lit panes again, so I left it at 0. The existing glints carry the highlight.
- GHOST (coherence #8): wave 2 already had the depth pre-pass (front surface only). This round: opacity 0.55 -> 0.72 and scan band 0.16 -> 0.08, so the city behind no longer prints through a stadium/mall preview. I checked this with a stadium ghost in iso-close (the --post setGhost via screenToTile).
**Checked, no change**
- AO midpoint: the rendered ratio against no-AO (one-bakery) is p1 0.653, p5 0.86, which is the ~0.65 cap. Uncapped it is p1 0.53. Soft pooling under the sills and cornice is visible and close to ref04's. Raising aoSkyShadow to 0.95 / reach 1.6 moved the render by <0.5% (the pooling is limited by the ray/broad terms, not the sky term), so I did not change it.
- Bridge deck (coherence #1): already fixed by roads in wave 2. roads.js draws the bridge surface, and the voxel deck top sits hidden 0.05 under it. voxel lotAsphalt renders darker than its albedo, so there is no near-black lift to fix.
**Measured**: iso-mid, iso-close and one-bakery all have 0 console errors. fps is load noise (load 9-17; 20/27/22 at dpr 2).
**Next**: if critics still call glass pale, go to glassDiffuse 0.4 before touching the tint. If "navy/dull", go back to glassDiffuse 0.55. If AO needs more pooling, raise aoRayStrength 0.55 -> 0.62 (the r13 note). The sky-shadow knobs barely move the render.

### Coordinator note (2026-09-26 16:40) — glass: STOP darkening
My older brief said glass reads pale → darken to ~#2a5aa8-#3a78c8. Now measured, our glass quartiles (#294c74…#a8dbfa) already match or exceed ref05's (#174766…#6cb6ce), and shops/downtown critics call it "flat dark navy, no reflections". Keep the base colour as is; if you touch glass, only add the reflection read: a lighter highlight band/streak on some panes and a slightly lighter top row, never a darker base.

## 2026-09-26 — wave 4 round 2 (builder)
Critic w4r1 picked the reference. Biggest gap: on the bakery the shaded right face (red, cream, storefront) was almost as bright as the lit left face, and there was almost no soft AO under the cornice, sills, awnings or sign. Secondary: the glass was "flat blue with drawn-on white streaks".
**Diagnosed** (new scratchpad tools: tools/colorprobe.js stacks 6-voxel slabs of chosen palette colours into one tower and returns the screen coords of each slab's left and right faces; tools/colratio.py samples them; tools/aomap.mjs rasterises a model's baked AO per face direction in node; tools/ab2.mjs is ab.mjs plus `post`/`noshot` per variant):
- Shade side: shadeSide and bounce carried the albedo hue as albedo/LUMA (clamped to 2.2). For a saturated colour that multiplies the dominant channel: red R x1.7 on the shade face. Probe right/left: cream 0.72, but red 1.06, pink 0.85, brick 0.91. The shaded red R was 252, the same as the lit face.
- Lit red and pink clip in post, not in materials. Lit red R stays 253 even with post exposure 0.5. The cause is post's luma-ratio ops: curve gamma 0.82 gives a low-luma colour y^-0.18 (x1.3-1.5), and the shade floor `d *= 1 + dy/y` gives about x1.7-2 at y ~0.15-0.25. Both scale every channel by a LUMA ratio, so a red's R channel runs into the knee. A guard that capped the gain by the peak channel (in-page shader patch, tools/pguard.js) barely moved the lit red, so something later in the chain also pushes it. I did NOT edit post.js.
- AO: aomap for bakery +z had median 0.65, p25 0.59, and the open wall only reached ~0.85, because broad + sky + floor all stacked. In the render, the red panel under the cornice was 175 vs 200 open vs 213 no-AO (sRGB). The crease was only 0.82 of no-AO: the shader aoWallCap 0.34 plus the post floor lift compressed it.
**Changed**
- materials.js: `shadeSide[3]` and `bounce[2]` (new, default 1) blend the hue carry to a PEAK-normalised hue (<= 1). The shade side keeps its dominant channel and drops the weak ones, like ref04's shaded orange. Probe right-face R: red 237 -> 189, pink 246 -> 206, dough 210 -> 184, brick 204 -> 163. Cream and white are unchanged (0.69-0.72). Blues: roofBlue R/L 0.71 -> 0.64, navy 0.63 -> 0.58. That is on the light target 0.63, but watch for "heavy navy".
- materials.js: `aoWallCap [0.34,0.12] -> [0.50,0.14]`. voxel.js: aoRayStrength 0.55 -> 0.62, aoWallFloor 0.55 -> 0.48, aoSkyShadow 0.8 -> 0.9, aoBroad 0.45 -> 0.35. New `aoSkyDepth 0.8 / aoSkyMin 0.2` scale each ledge's sky-shadow reach by its own upward darkness, so a deep cornice or awning pools wide, a 1-voxel sill pools short, and the panel between stays open. The bakery cornice crease went 0.82 -> 0.74 of no-AO (sRGB), with a longer soft ramp. The storey-overhang, sign-band and storefront corners read set-in, and nothing looks muddy (r2/a3_cmp.png).
- materials.js glass: glassSheen 0.42 -> 0.30. The thin second glint stripe is gone (glassHiP.w 0.40 -> 0), and a gentle head gloss is on (glassHiP.x 0 -> 0.20). Glass p50 (49,96,146) -> (47,90,140), p97 (157,187,251) -> (150,181,251). ref04 glass p50 is (46,77,122).
- Coherence #8 (ghost) and #1 (bridge deck) were already handled in wave 2 / w4r1. No change this round.
**Measured**: check.sh clean. voxel selfTest passes (node). iso-mid, iso-close and one-bakery have 0 console errors. fps is 23/26/23 (load 12, dpr 2: noise). Bakery mesh time in node is ~unchanged (250 ms).
**Not mine (POST, please route)**: saturated reds and pinks clip on the LIT face (R 254) because the curve gamma and the shade floor are luma-ratio gains. A lit red and its AO dips therefore sit at the clip, and AO on lit red walls is invisible. Suggest capping those gains by the peak channel (the gain must not push max(r,g,b) past ~0.9), or keying the floor kernel on max(y, 0.5*peak). Small movers (VOX_SMALL) also inherit aoRayStrength 0.62 (was 0.55). They take a slightly deeper crease only, since broad/sky/floor stay gated off below aoDist 1.
**Next**: if a critic calls the shade faces heavy or navy, set shadeSide[3]/bounce[2] to 0.6 (the probe midpoint: roofBlue 0.67, navy 0.60). If the facade reads dirty, go to aoWallCap 0.45 first, then aoRayStrength 0.58. If the glass is called dull/flat, go to glassSheen 0.36 before the stripe comes back.

### Coordinator note (2026-09-26 18:40) — after w4r2
Two causes of "dirty contact shading" are NOT yours and are routed to light (stair-stepped shadow edges, streaks on the plinth side). Yours: (1) the lot plinth's shaded side band reads near-black — lift C.lotSide (core.js lot colours are shared; surface may adjust them, note it) so the shaded side stays a coloured mid-grey like ref04's plinth; (2) almost no AO in upper-wall inside corners under cornices — widen/soften the AO pooling there (target a smooth pale gradient, ref04). Also note the light builder is lifting the global midtones (post curve median 0.45→0.55), which will brighten shaded sides overall — re-measure after that lands before over-lifting.

## 2026-09-26 — wave 4 round 3 (builder)
Critic w4r2 picked the reference. Biggest gap: dirty contacts at the bakery base (near-black plinth side band, a black lot strip at the wall foot, stepped shadow edges on the paving). Secondary, agreed with w4r1: almost no AO in the upper walls' inside corners, under the cornice or around the window bays.
**Diagnosed** (scratchpad r3/: pick.js raycasts a screen pixel to its mesh; v1-v6 in-page A/B; ao/ aomap bakes). The "plinth band" at the bakery's right edge (x=2000 in one-bakery) is three stacked things:
- (a) The voxel lotPlinth side, y 0.42-0.92, rendered 37-48. The unoccluded lotSide right face (colour probe) is 133. Without voxel AO it is 75, and with lighting `wallFillAway 0` it is 146.
- (b) Terrain's LOT_Y footing under it, y 0-0.42, rendered 9-17. This is **not voxel**. With lighting worldAO off it is 20, with wallFillAway 0 it is 69, and with both off it is 106.
- (c) The lot-top strip beside the wall foot, rendered 22. It is 129 with lighting worldAO off.
Also: voxel groundAO treated y<0 as a floor, but the lot stands on the footing, so the plinth side took a full wall-meets-floor crease (baked 0.49 at the bottom).
**Changed**
- voxel.js: new LOT GROUND (`lotGroundDrop 0.42`, `_lotGroundDropV`). A building-scale model (aoDist >= 1, footprint >= 2x2 units) whose y=0 layer is >= 97% solid (a lotPlinth) gets its AO ground plane dropped LOT_Y OUTSIDE the footprint, and the footprint stays solid down there (the footing). This applies to both the ray occupancy and the broad cone trace's analytic ground. The baked plinth side went 0.78->0.49 before and 0.89->0.70 now. Props, cars and trees are unaffected, because they never fill their footprint.
- voxel.js: aoSkyShadow 0.9 -> 1.0, aoSkyReach 1.25 -> 1.6, aoSkyMin 0.2 -> 0.5, so thin sills, belts and flower boxes now lay a visible soft pool. materials.js: aoDirect 1.25 -> 1.4. I tried aoSkyShadow 0.45 (the aomap looked cleaner) and it REMOVED the under-cornice pooling in-render (r3/b_zoom.png), so the sky shadow is what carries the ledge read. Keep it.
- Glass: no change (coordinator 16:40, stop darkening). Ghost #8 and bridge #1 were already done.
**Measured**: check.sh clean, and the voxel selfTest passes (node). iso-mid, iso-close and one-bakery have 0 console errors. fps is load noise (load 15; 7/12/19 at dpr 2). Voxel plinth side 48 -> ~59 (a small gain; the rest is lighting). The bakery's right upper wall now pools softly under the bay sill and cornice.
**Not mine, routed in coherence.md**: the black footing (b) and the lot strip (c) are lighting.js (`wallFillAway 0.80` on terrain's footing side, and worldAO on the lot top). Those two, not voxel AO, are most of the "near-black plinth band" the critic sees.
**Next**: if the ledges are called dirty, go to aoDirect 1.3 before touching the sky terms. If the plinth still reads dark after light fixes its footing, lower shadeSide depth on low away faces.

### Coordinator note (2026-09-26 20:55) — after w4r3: 3 critics agree on corner AO
w4r1, w4r2, w4r3 all say: too little soft AO in inside corners, under cornices/parapets and around window/door frames. Your plinth-band diagnosis is right and routed; now make the facade AO unmistakable. Measure ref04 yourself with PIL: the wall value directly under the cornice and in window reveals vs open wall mid-face (my eyeball: ~0.65-0.75 of the open wall, fading over ~3-4 fine voxels, warm-tinted not grey). Match that on one-bakery at the same object scale and log both numbers. The "washed-out right face" in w4r3 is the global midtone lift (light builder) — don't fight it with albedo; AO in the right places will add the sculpted read.
